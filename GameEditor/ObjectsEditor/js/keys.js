'use strict';

import {KEYBOARD} from "../../../GameEngine/InputManager/KeyboardController.js";

export const OBJECT_EDITOR_KEYS = {
	COMMAND: {
		KEYBOARD: [
			KEYBOARD.COMMAND,
		],
	},
	BACKSPACE: {
		KEYBOARD: [
			KEYBOARD.BACKSPACE,
		],
	},
}
