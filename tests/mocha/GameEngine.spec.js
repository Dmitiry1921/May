'use strict';

import assert from 'node:assert/strict';
// import GameEngine from '../../js/class/GameEngine.js';


describe.skip("#GameEngine", () => {
  beforeEach(() => {
  });
  describe(`#constructor`, () => {
    it('should ', async () => {
      const game = new GameEngine();

      const res = game.setLevel(0);
      console.log(res);
    });
  });
});
